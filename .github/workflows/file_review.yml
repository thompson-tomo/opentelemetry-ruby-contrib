name: File Review

on:
  workflow_dispatch:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  merge_group:

permissions:
  contents: read

jobs:
  filesystem:
    uses: ./.github/workflows/file_review-filesystem.yml
